{% extends "::base.html.twig" %} {% block title %}AppBundle:FrontController:index{% endblock %}

{% block body %}

<div class="container-fluid container-admin">
    <div class="col-md-12">
        <div class="row">
            <div class="col-md-4">
                <a class="style-btn-text-back-home" href="{{ path('article_index') }}">Manage articles</a>
                </div>
                <div class="col-md-4">
                    <a class="style-btn-text-back-home" href="{{ path('comment_index') }}">Manage Comments</a>
                </div>
                <div class="col-md-4">
                    <a class="style-btn-text-back-home" href="{{ path('user_index') }}">Manage Users</a>
                </div>
            </div>
        </div>
    </div>
</div>




{% endblock %}